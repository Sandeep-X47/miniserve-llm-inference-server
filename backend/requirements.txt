fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
prometheus-client==0.21.1
httpx==0.28.1
# Only needed when ENGINE=hf (real model). Mock mode runs without these.
# torch
# transformers
# matplotlib  # for benchmark.py graph output
